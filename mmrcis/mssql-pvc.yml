apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mmrcis-mssql-data
  labels:
    app: mmrcis-mssql-data
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
